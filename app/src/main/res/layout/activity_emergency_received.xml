<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1A1A2E">

    <!-- Compact Emergency Header -->
    <LinearLayout
        android:id="@+id/headerLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/tvEmergencyType"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#FF1744"
            android:gravity="center"
            android:paddingVertical="10dp"
            android:paddingHorizontal="12dp"
            android:text="⚠️ ACİL DURUM"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            android:textStyle="bold" />

        <!-- Custom description (only visible for custom emergencies) -->
        <TextView
            android:id="@+id/tvCustomDesc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#4A1F6A"
            android:gravity="center"
            android:paddingVertical="4dp"
            android:paddingHorizontal="12dp"
            android:text=""
            android:textColor="#E1BEE7"
            android:textSize="12sp"
            android:visibility="gone" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:paddingVertical="6dp"
            android:paddingHorizontal="12dp"
            android:background="#16213E">

            <TextView
                android:id="@+id/tvSenderName"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Kullanıcı Adı"
                android:textColor="@android:color/white"
                android:textSize="15sp"
                android:textStyle="bold" />

            <!-- Mute + Cancel inline -->
            <Button
                android:id="@+id/btnMuteAlarm"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:layout_marginEnd="6dp"
                android:backgroundTint="#FF9800"
                android:text="🔇 Sustur"
                android:textColor="@android:color/white"
                android:textSize="11sp"
                android:paddingHorizontal="8dp" />

            <Button
                android:id="@+id/btnCancelEmergency"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:backgroundTint="#F44336"
                android:text="❌ İptal"
                android:textColor="@android:color/white"
                android:textSize="11sp"
                android:paddingHorizontal="8dp"
                android:visibility="gone" />

        </LinearLayout>

    </LinearLayout>

    <!-- Map View — fixed reasonable height -->
    <org.osmdroid.views.MapView
        android:id="@+id/mapView"
        android:layout_width="match_parent"
        android:layout_height="220dp"
        app:layout_constraintTop_toBottomOf="@id/headerLayout" />

    <!-- Tab Layout -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#16213E"
        app:tabTextColor="#AAAAAA"
        app:tabSelectedTextColor="@android:color/white"
        app:tabIndicatorColor="#FF1744"
        app:layout_constraintTop_toBottomOf="@id/mapView" />

    <!-- ViewPager for Tabs — fills remaining space -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tabLayout" />

</androidx.constraintlayout.widget.ConstraintLayout>